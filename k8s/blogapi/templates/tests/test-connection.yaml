apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "blogapi.fullname" . }}-test-connection"
  labels:
    {{- include "blogapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "blogapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
