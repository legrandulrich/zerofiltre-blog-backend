apiVersion: v1
kind: Service
metadata:
  name: {{ include "blogapi.fullname" . }}
  labels:
    {{- include "blogapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "blogapi.selectorLabels" . | nindent 4 }}
